{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "dev": "cd client/&& npm start",
    "lint": "eslint . --ext .js",
    "doc": "npx jsdoc -c jsdoc.json -r",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bad-words": "^3.0.3",
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "express-validator": "^6.14.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.2",
    "nodemon": "^2.0.16",
    "socket.io": "^4.5.1",
    "validator": "^13.7.0"
  },
  "not working comments": {
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon src/server.ts",
      "lint": "eslint . --ext .ts",
      "test": "echo \"Error: no test specified\" && exit 1",
      "build": "npx -p typescript tsc",
      "doc": "npx jsdoc -c jsdoc.json -r",
      "bundle": "copy \"package.json\" \"dist/package.json\" && cd dist && \"C:\\Program Files\\7-Zip\\7z.exe\" a -tzip ../bundle.zip *"
    }
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "jsdoc": "^4.0.2"
  }
}
